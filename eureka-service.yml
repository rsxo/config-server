spring:
  profiles:
    active: default
  cloud:
    config:
      uri: http://configserviceasasa:8888
server:
  port: 8762
eureka:
  client:
     register-with-eureka: false
     fetch-registry: false
     #service-url:
     #      defaultZone: http://${discovery-service:localhost}:8761/eureka/
